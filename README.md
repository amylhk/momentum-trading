# Momentum Trading

Public Streamlit stock-analysis app.

Run locally with:

```bash
python -m streamlit run app.py
```

The app uses `requirements.txt` for its Python dependencies and does not
include the private project's offline knowledge-base tooling.
